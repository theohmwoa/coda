{
  "name": "coda-app",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "End-user UI for coda — skill cards, live status, result cards, agent-feedback-ui approval surfaces.",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "marked": "^14.1.3",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "//": "agent-feedback-ui components vendored in src/lib/agent-feedback/ until upstream ships a built dist/ (currently the repo's package.json files-list only includes dist which doesn't exist in git). Switch back to github:theohmwoa/agent-feedback-ui once 0.1.0 is published.",
  "devDependencies": {
    "@types/node": "^25.8.0",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.3",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
