# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Traces & runs
traces/
*.coda
*.jsonl
runs/
.runs/

# but DO keep the committed sample run that ships with the example
!examples/sample_run/*.jsonl
!examples/sample_run/*.md

# Secrets
.env
.env.local

# Jupyter
.ipynb_checkpoints/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp

# Claude Code session state
.claude/

# UI build / Node
app/node_modules/
app/dist/
# but DO ship the built UI bundle so `python -m coda serve` works
# out of a fresh `pip install -e .` without needing Node.
# src/coda/static/app/ is intentionally NOT ignored.
