"""tau-bench airline integration — exercises ctx + ContextPolicy against
realistic-shape data from Sierra's tau-bench airline domain.

Imports the airline tools and data from a cloned tau-bench repo (path
configurable via TAU_BENCH_PATH env var; defaults to ../../tau-bench
relative to the coda repo root).
"""
